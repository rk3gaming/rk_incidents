{
  "name": "rk-experimental",
  "version": "1.0.0",
  "description": "TypeScript FiveM project",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild src/client/*.ts src/server/*.ts --bundle --platform=browser --outdir=build --outbase=src --format=iife",
    "dev": "npx esbuild src/client/*.ts src/server/*.ts --bundle --platform=browser --outdir=build --outbase=src --format=iife --watch"
  },
  "keywords": [
    "fivem",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@communityox/ox_lib": "^3.30.10",
    "@communityox/oxmysql": "^1.4.3",
    "@nativewrappers/fivem": "^0.0.103"
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "esbuild": "^0.19.0",
    "typescript": "^5.8.3"
  }
}
